# Dispersive_XAS example

This folder mirrors the current large-quantity analysis workflow script:

- `20260227_analysis.py` (copied from `Python_codes/02_analysis_for_large_quantity/20260227_analysis.py`)

Note:
- Exported outputs (`.html/.csv/.json`) are intentionally not tracked here.
- Raw detector `.h5` files are intentionally not tracked here because they are large.
- Keep this example script synchronized with future updates to the source analysis script.
